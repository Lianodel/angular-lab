import { SolarSystemDirective } from './solar-system.directive';

describe('SolarSystemDirective', () => {
  it('should create an instance', () => {
    const directive = new SolarSystemDirective();
    expect(directive).toBeTruthy();
  });
});
